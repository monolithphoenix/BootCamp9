import React from 'react';
// import PropTypes from 'prop-types';
import './ContainerWeight.css'

const ContainerWeight = props => {
    return (
        <div className="ContainerWeight">
            WEIGHT conteiner content will be here
        </div>
    );
};

ContainerWeight.propTypes = {
    
};

export default ContainerWeight;